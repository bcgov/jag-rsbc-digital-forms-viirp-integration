	@Override
	public CreateImpoundmentServiceResponse createImpoundment(String correlationId, CreateImpoundment impoundment) {
		
		return webClient
                .post()
                .uri("/cases/impoundments")
                .headers (headers -> headers.setBasicAuth(properties.getVipsRestApiUsername(), properties.getVipsRestApiPassword()) )
        		//.defaultHeaders( defaultHeaders -> 
        		//		headers.set("smgov_userguid", properties.getVipsRestApiCredentialsGuid())
        		//		
        		//)
        				
//        			headers.set("smgov_userguid", properties.getVipsRestApiCredentialsGuid());		
//        			headers.set("smgov_userdisplayname", properties.getVipsRestApiCredentialsDisplayname());
//        			headers.set("sm_user", properties.getVipsRestApiCredentialsUser()) };
                .body(Mono.just(impoundment), CreateImpoundment.class)
                .retrieve()
                .bodyToMono(CreateImpoundmentServiceResponse.class)
        		.block();
		
	}